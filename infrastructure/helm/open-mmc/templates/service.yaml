apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-mmc.fullname" . }}
  labels:
    {{- include "open-mmc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-mmc.selectorLabels" . | nindent 4 }}
