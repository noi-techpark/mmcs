# Default values for open-mmc.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/noi-techpark/mmcs/open-mmc
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}
podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}

# Extra environment variables for the container, on top of ADDR (which is
# always set from service.port). See backend/main.go for what it reads.
env: {}

# Same as env, but rendered into a Kubernetes Secret and consumed via
# secretKeyRef instead of a plain value — for anything sensitive (e.g.
# ODH_CLIENT_SECRET). CI sets these at deploy time; see
# .github/workflows/main.yml and infrastructure/helm/test.yaml.
secretEnv: {}
